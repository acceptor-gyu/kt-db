# Local Development Profile
# Usage: ./gradlew bootRun --args='--spring.profiles.active=local'

# DB Server connection (로컬 개발 환경)
db.server.host=localhost
db.server.port=9000

# Logging (개발용 - 상세 로그)
logging.level.root=INFO
logging.level.study.db.api=DEBUG
logging.level.study.db.api.client=DEBUG
